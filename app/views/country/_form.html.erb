<%= form_for @country, url: '/country', html: { method: :post } do |f| %>
  <% if @country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country.errors.count, "error") %> prohibited this instance from being saved:</h2>

      <ul>
      <% @country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "ISO Alpha2 Code" %><br>
    <%= f.text_field(:alpha2, {:max => 2}) %> 
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
